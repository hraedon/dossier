---
model: glm-5.2
datetime: 2026-07-26T06:30Z
project: dossier
---

# Session Reflection 3 — 2026-07-26 (full-E posture)

**Work summary:** Closed the two items session 2 deliberately deferred: switched
the lint select from `E4/E7/E9` to full `E` (enforcing E501 at 100) and removed
the `N806`/`N817`/`N818` ignore via real renames. Fixed all 38 E501 violations by
wrapping (no `noqa`); renamed `LoginRequired`→`LoginRequiredError` (5 sites),
lowercased the function-local constants `_DASHBOARD_MAX_ITEMS`,
`_SCHEMA_RE`, `_RESERVED_SCHEMAS`, and the test actor `AGENT_GLM_2`; dropped the
`Settings as S` acronym alias. dossier's select now exactly matches regista/cairn:
`["E","F","I","N","W","UP","RUF"]`.

---

## On the project

dossier was the cleanest of the three targets: full `E` added *only* E501 (no
other E-rule surprises), and the 6 naming violations were genuine, fixable debt
the prior session correctly flagged as "rename-the-debt, not ignore." Doing the
renames closes the dossier↔regista/cairn posture divergence that session 2
called out as a gap. The `LoginRequiredError` rename is the right call — it's a
caught-and-handled exception, and the `Error` suffix is the family/PEP-8 norm.

## On the work done

Confident in: ruff 0.16 fully clean (full E + N, zero ignores), `mypy --strict`
clean (30 files), 649 passed / 17 skipped (DB-gated) / 1 xfailed — no behavior
change, all wraps are mechanical. Every E501 was a real wrap (multi-line call,
parenthesized string concat, or wrapped signature); no `# noqa: E501` escapes.

Less confident / caveats: the em-dash detail string in `notifications.py` got
split across an implicit-concat boundary mid-message — cosmetically slightly
awkward but semantically identical. The `_login_required_handler` function name
was kept (only the exception class was renamed); renaming the handler too would
be churn without benefit.

## On what remains

- Nothing lint-shaped. dossier matches regista/cairn's posture exactly.
- The pre-existing adversarial-review deferred items (env-var admin gate,
  audit-mode permits-after-logging) are unchanged — out of scope for a lint pass.

## Gaps to flag

- **`LoginRequiredError` is a public-ish symbol** (raised in two HTML-route
  dependencies, caught by a registered handler). The rename is internal to this
  repo (no external API consumers documented), but if any out-of-repo caller
  catches `LoginRequired` by name it would break — flagged for the publication
  review, not a concern for the current deployment.
- The family-wide SIM/PLW decision (skip SIM everywhere; agent-suite adopts
  PLW1510 alone) is documented in the agent-suite reflection for this session.

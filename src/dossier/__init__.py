"""dossier — a human-facing work-item tracker that is also a provenance instrument.

Thin UI + auth + workflow over regista. dossier owns no work-state schema; see
docs/provenance-model.md for the contract and plans/001-mvp.md for the build order.
"""

__version__ = "0.0.1"
